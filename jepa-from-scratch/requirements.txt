# Recreate the environment with uv (recommended):
#   uv venv --python 3.12 .venv
#   uv pip install --python .venv -r requirements.txt
#
# PyTorch wheels are CUDA-enabled by default on Linux x86_64. For a CPU-only
# machine, install torch/torchvision from the CPU index instead:
#   uv pip install --python .venv torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# (Pins reflect the versions these notebooks were verified on; newer patch
#  releases are generally fine. PyTorch has no wheels for Python 3.14 yet — use 3.12.)
torch==2.12.0
torchvision==0.27.0
numpy==2.4.6
matplotlib==3.11.0
jupyter
ipykernel
