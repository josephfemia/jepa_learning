import React from "react";
import ReactDOM from "react-dom/client";
import JepaCourse from "./JepaCourse.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JepaCourse />
  </React.StrictMode>
);
